---
name: ask-intern
description: "Use when reading large files, multiple medium/large files, broad codebase context, long logs, diffs, transcripts, generated output, or when drafting repetitive tests, fixtures, docs, config, or boilerplate."
---

# ask-intern

Use the canonical `## Token Delegation` rule from `~/.claude/AGENTS.md` / `~/.codex/AGENTS.md`; it is already loaded in normal sessions.

If the exact thresholds or examples are not in context, read only that section, then delegate with `ask-intern` instead of reading bulky inputs directly. Keep exact code, line numbers, and edit snippets in direct narrow reads after the summary.
